import RevbeamLandingPage, {
  metadata as landingMetadata,
} from '@/components/marketing/revbeam-landing'

export const metadata = landingMetadata

export default function MillianLandingPage() {
  return <RevbeamLandingPage />
}
